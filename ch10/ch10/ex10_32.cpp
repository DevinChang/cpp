#include <iostream>
#include <vector>
#include <string>
#include <iterator>
#include <algorithm>
#include <numeric>
//#include "exm_7_2._11_21.h"


//bool compareIsbn1(Sales_data &item1, Sales_data &item2){
//	return item1.isbn() < item2.isbn();
//}

int main17(){
	
	//std::istream_iterator<Sales_data> item_iter(std::cin), eof;
	//std::ostream_iterator<Sales_data> out_iter(std::cout, "\n");

	//std::vector<Sales_data> vec;
	//while (item_iter != eof)
	//	vec.push_back(*item_iter++);
	//std::sort(vec.begin(), vec.end(), compareIsbn1);
	//for (auto beg = vec.cbegin(), end = beg; beg != vec.cend(); beg = end){
	//	end = std::find_if(vec.begin(), vec.end(), [beg](Sales_data &item){ return item.isbn() != beg->isbn(); });
	//	//std::cout << std::accumulate(beg, end, Sales_data(beg->isbn())) << std::endl;
	//}
	

	system("pause");
	return 0;
}